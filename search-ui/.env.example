# Base URL of the search-api backend. Copy to .env (dev) or .env.production (build).
# Local dev only — production leaves this unset so API calls are relative (proxied by nginx)
VITE_API_BASE=http://localhost:8092
